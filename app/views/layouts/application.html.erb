<!DOCTYPE html>
<html>
<head>
  <title>ChatFlow</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <%# expose current user id for JS %>
  <% if user_signed_in? %>
    <meta name="current-user-id" content="<%= current_user.id %>">
  <% end %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
</head>

<body class="min-h-screen bg-gray-50 text-gray-900">
<nav class="bg-white border-b px-4 py-3 flex items-center justify-between">
  <div class="font-semibold">💬 ChatFlow</div>
  <div class="space-x-3">
    <%= link_to "Conversations", conversations_path, class: "text-blue-600 underline" %>
    <% if user_signed_in? %>
      <%= link_to "Profile", "/users/me", class: "text-blue-600 underline" %>
      <%= button_to "Sign out", destroy_user_session_path, method: :delete, class: "px-3 py-1 rounded bg-gray-200" %>
    <% else %>
      <%= link_to "Sign in", new_user_session_path, class: "text-blue-600 underline" %>
      <%= link_to "Sign up", new_user_registration_path, class: "text-blue-600 underline" %>
    <% end %>
  </div>
</nav>

<main class="max-w-3xl mx-auto p-4">
  <%= yield %>
</main>
</body>
</html>
